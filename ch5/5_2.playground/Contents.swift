import UIKit

/*
 연산자 우선순위와 결합방향(위에서부터 아래로, 왼쪽에서부터 오른쪽으로)
 (), [], . , ->
 sizeof, &, *, +, -, ~, !
  *, /, %
 +, –
 <<, >>
 <, <=, >, >=
  ==, !=
 &
 ^
  |
 &&
 ||
 ?:
 =, +=, -=, *=, /=, %=, <<=, >>=, &=, ^=, |=
 ,
 */
